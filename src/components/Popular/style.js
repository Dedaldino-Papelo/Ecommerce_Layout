import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginVertical: 20
    },  
    Popular: {
        marginHorizontal: 30,
        flexDirection: "row",
        flexWrap: "wrap",
        gap: 25
    },
})

export default styles